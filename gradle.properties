# Sets default memory used for gradle commands.
org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false

# MC 1.21.1 requires Java 21; pin Gradle to the JDK 21 install on this machine
# so `gradlew` works regardless of what JAVA_HOME points at in the user shell.
org.gradle.java.home=C:\\Program Files\\Java\\ms-21.0.10

geckolib_version=4.7
